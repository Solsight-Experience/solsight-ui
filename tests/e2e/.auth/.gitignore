# This directory is auto-generated by the Playwright auth setup step.
# It stores the serialised browser storage state (cookies + localStorage)
# so all subsequent tests start as an authenticated user.
#
# DO NOT commit user.json — it contains session tokens.
user.json
